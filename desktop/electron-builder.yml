appId: com.deeplan.desktop
productName: DeEplan
copyright: DeEplan

# Pas d'auto-update ni de publication par electron-builder : la Release GitHub
# est gérée par le workflow. (Évite le crash `updateInfoBuilder` sans remote.)
publish: null

directories:
  output: dist
  buildResources: build

files:
  - main.js
  - package.json

# Pas de signature (compte Apple/certificat Windows non requis pour cette phase).
mac:
  target: dmg
  category: public.app-category.productivity
  identity: null

win:
  target: nsis

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true

linux:
  target: AppImage
  category: Office
